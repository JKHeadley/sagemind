{
  "name": "ledgerwise",
  "version": "0.1.0",
  "description": "LedgerWise — double-entry bookkeeping on Google Apps Script",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp push && clasp deploy",
    "open": "clasp open --webapp",
    "logs": "clasp logs --watch",
    "login": "clasp login"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83"
  }
}
